@model IEnumerable<SporSalonuYonetimi.Models.Appointment>

@{
    ViewData["Title"] = "Randevularım";
}

<div class="container mt-5">
    <h2 class="text-primary mb-4">📅 Randevularım</h2>

    @if (!Model.Any())
    {
        <div class="alert alert-warning">
            Henüz hiç randevunuz bulunmamaktadır. <a asp-controller="Home" asp-action="Index">Hemen Randevu Al!</a>
        </div>
    }
    else
    {
        <div class="card shadow-sm">
            <div class="card-body">
                <table class="table table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Tarih & Saat</th>
                            <th>Hizmet</th>
                            <th>Antrenör</th>
                            <th>Ücret</th>
                            <th>Durum</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.AppointmentDate.ToString("dd.MM.yyyy HH:mm")</td>
                                <td>@item.Service?.ServiceName</td>
                                <td>@item.Trainer?.FullName</td>
                                <td>@item.Service?.Price ₺</td>
                                <td>
                                    @if (item.IsRejected)
                                    {
                                        <span class="badge bg-danger">Reddedildi</span>
                                        <div class="text-danger small mt-1" style="font-size: 0.75rem;">
                                        </div>
                                    }
                                    else if (item.IsCancelled)
                                    {
                                        <span class="badge bg-secondary">İptal Edildi</span>
                                    }
                                    else if (item.IsConfirmed)
                                    {
                                        <span class="badge bg-success">Onaylandı</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-warning text-dark">Bekliyor</span>
                                    }
                                </td>

                                <td>
                                    @if (!item.IsCancelled && item.AppointmentDate > DateTime.Now)
                                    {
                                        <form asp-action="Cancel" asp-route-id="@item.AppointmentId" method="post">
                                            <button type="submit" class="btn btn-sm btn-outline-danger"
                                                onclick="return confirm('Randevunuzu iptal etmek istiyor musunuz?');">
                                                Randevuyu İptal Et
                                            </button>
                                        </form>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>