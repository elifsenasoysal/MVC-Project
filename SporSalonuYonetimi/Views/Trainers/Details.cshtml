@model SporSalonuYonetimi.Models.Trainer

@{
    ViewData["Title"] = "Antrenör Detayları";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">

            <div class="card shadow">
                <div class="card-header bg-info text-white" >
                    <h4>👤 Antrenör Detayları</h4>
                </div>

                <div class="card-body">

                    <dl class="row mb-0">
                        <dt class="col-sm-4 fw-bold">Ad Soyad</dt>
                        <dd class="col-sm-8">@Model.FullName</dd>

                        <dt class="col-sm-4 fw-bold">Uzmanlık Alanı</dt>
                        <dd class="col-sm-8">@Model.Specialization</dd>

                        <dt class="col-sm-4 fw-bold">Çalışma Saatleri</dt>
                        <dd class="col-sm-8">
                            <span class="badge bg-secondary">
                                <i class="bi bi-clock"></i>
                                @Model.WorkStartHour:00 - @Model.WorkEndHour:00
                            </span>
                        </dd>

                        <dt class="col-sm-4 fw-bold">Verebildiği Dersler</dt>
                        <dd class="col-sm-8">
                            @if (Model.Services != null && Model.Services.Any())
                            {
                                @foreach (var service in Model.Services)
                                {
                                    <span class="badge bg-primary me-1">
                                        @service.ServiceName
                                    </span>
                                }
                            }
                            else
                            {
                                <span class="text-muted small">
                                    <i class="bi bi-exclamation-circle"></i>
                                    Ders atanmadı
                                </span>
                            }
                        </dd>
                    </dl>

                    <div class="d-grid gap-2 mt-4">
                        <a asp-action="Edit"
                           asp-route-id="@Model.TrainerId"
                           class="btn btn-warning">
                            Düzenle
                        </a>

                        <a asp-action="Index"
                           class="btn btn-secondary">
                            Listeye Dön
                        </a>
                    </div>

                </div>
            </div>

        </div>
    </div>
</div>
